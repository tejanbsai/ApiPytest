[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --strict-markers
    --junitxml=reports/results.xml
    --html=reports/report.html
    --self-contained-html

markers =
    smoke: Core happy-path tests
    security: Auth enforcement tests
    negative: Invalid input tests
    boundary: Boundary/edge case tests
